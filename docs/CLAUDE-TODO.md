# Out-of-scope follow-ups (captured, not built)

Spotted during scoped batch work. Do not fix inline in the batch that found it.

## From Batch 2 (v3 doc-detail + graph reskin, 2026-05-16)

- **doc-detail header action row overflows horizontally at mobile widths (<420px).** The PageHeader actions slot holds 5 controls (Graph, PDF, Report, Obligations, Export) which do not wrap on a 393px viewport. Consistent with the rest of v3 (BRIEF locks v3 to desktop 1280px+; shipped Batch 1 dashboard/obligations behave the same). Not a Batch 2 regression. Belongs to a future v3 responsive/mobile pass, not Batch 3 or 4 as currently scoped.

## From Batch 4 (v3 finalise, 2026-05-16)

- ~~**`frontend/src/components/demo-video/styles.ts:26` hardcodes `display: "'Cormorant Garamond', 'Playfair Display', Georgia, serif"`.**~~ **RESOLVED in the v3 landing reskin (2026-05-16, branch `feat/v3-landing-reskin`).** `styles.ts` `fonts.display`/`body`/`mono` now resolve to the Geist stack and the `colors` palette was aligned to the v3 tokens; no serif font string remains anywhere in `frontend/src`. It was landing-only and therefore in scope for that follow-on.
- **Light-theme chart-token polish.** A few Recharts elements on `/analytics` keep hardcoded dark values (the "Clause distribution" tooltip `contentStyle` `background:#1d1d22`, and the `#a1a1aa` axis tick fill). They render dark-on-light when the light theme is active. Identical to the shipped pilot (the Batch 4 promote was relocate-only and byte-equivalent), so out of Batch 4 scope. Belongs to the deferred light-theme polish pass (PROJECT-PLAN §2 marks light-theme polish beyond a single-pass token map as out).
- **`docs/v1-audit/capture-v3.mjs` and `capture-v3-debug.mjs` still point at `localhost:3000/analytics-v2`.** That route no longer exists after the Batch 4 promote. These are one-shot historical screenshot-capture scripts (their captures are already taken); not app code, out of the `frontend/src` grep scope. Repoint to `/analytics` only if these scripts are ever re-run.

## From the v3 landing reskin (2026-05-16, post-Batch-4 follow-on)

- **Light-theme contrast on the HeroVisual decorative knowledge-graph mock.** `frontend/src/app/hero-visual.tsx` renders five entity hues (`#60a5fa`, `#34d399`, `#a78bfa`, `#facc15`, `#fb923c`) as 9px SVG text labels at `opacity 0.75` and 6px legend dots directly on `.v3-card`, which is `#ffffff` under `html.light`. `#facc15` on white is ~1.5:1, under the WCAG 1.4.11 3:1 graphical-object minimum. Confined to the decorative product-preview *mock* (the brief classifies HeroVisual internals as an illustrative depiction of the product; no real page copy is affected - all body text uses inverting `var(--v3-text-*)`). The same v3 risk hexes already render on white in the shipped Batches 1-4. Out of scope per the brief's "depicts the product" allowance and PROJECT-PLAN §2 deferring light-theme polish beyond the single-pass map. Fix when the deferred light-theme polish pass runs: darken the five entity hexes one shade so they clear 3:1 on both surfaces, or theme-gate them.
- **`frontend/src/components/DemoVideoModal.tsx` is dead code using pre-v3 tokens.** Defined, never imported anywhere. Still uses `text-accent`/`bg-ink-*`. Pre-existing, not introduced by the landing reskin, and out of its scope (the brief lists only the `demo-video/` set + `HeroVideoPlayer` + `ScreenshotShowcase`). Either delete the unused file or reskin it if it is ever wired up.

## From the v3 mobile-responsive shell pass (2026-05-16, branch `feat/v3-mobile-responsive`)

- ~~**Per-page dense-grid reflow for `/dashboard` and `/deals` on phones.**~~ **RESOLVED in the per-page mobile pass (2026-05-16, branch `feat/v3-mobile-pages`).** Added additive media-gated utilities (`.v3-grid-resp` 768->2col 480->1col, `.v3-split-resp` 900->1col, `.v3-table-min` scroll region <=768, `.v3-pageheader`/`-actions` stack+wrap) and the `PageHeader` primitive now stacks title/actions on phones (fixes the Batch 2 doc-detail action-row overflow for every route at once). Wired into `/dashboard`, `/deals`, and `/documents/[id]` (KPI/risk grids, the contracts table, the 1fr/360px and 240px/1fr splits). Desktop is byte-for-byte unchanged (every rule is `@media`-gated, no base rule, `!important` only to beat the inline desktop value); verified 393px = 1-col stacked + PageHeader column + zero viewport overflow, 1440px = identical to before. Fresh-context opus review: OVERALL PASS, no blockers.
- **doc-detail export dropdown can clip on a ~393px phone.** `frontend/src/app/documents/[id]/page.tsx` ~L462: the Export menu is an absolutely-positioned panel with a fixed-ish ~200px width anchored right; on a narrow viewport it can clip the right edge. Pre-existing, untouched by and out of scope for the per-page mobile pass (flagged by the opus reviewer as informational). Belongs to a doc-detail-specific mobile follow-up: make the dropdown a bottom-sheet or width-clamped/repositioned on phones.
- **Mobile drawer has no focus trap / focus management.** `components/v3/shell.tsx`: when the off-canvas nav opens on mobile, focus is not moved into `#v3-sidenav`, Tab is not trapped, and the background `<main>` is not `inert`/`aria-hidden` (WCAG 2.4.3 / 2.1.2). Flagged Low by the fresh-context opus reviewer and intentionally not fixed in this PR: the codebase's other overlays (the ⌘K palette, ScreenshotShowcase cinema mode) also have no formal focus trap, so adding one only to the drawer would be inconsistent scope creep. Belongs to a dedicated overlay-a11y pass covering all three overlays consistently.

## From Batch 3 (v3 search + compare + deals reskin, 2026-05-16)

- ~~**The v3 shell's fixed 240px sidebar causes horizontal overflow at mobile width (393px) on every v3 surface.**~~ **RESOLVED in the v3 mobile-responsive shell pass (2026-05-16, branch `feat/v3-mobile-responsive`).** The fixed 240px `.v3-side` now becomes an off-canvas drawer below 1024px (hamburger in the existing topbar + scrim + body-scroll-lock + close on nav/scrim/Escape/route-change); desktop >=1024px is byte-for-byte unchanged. Verified at 393px: `/`, `/analytics`, `/dashboard` have zero horizontal viewport overflow; the drawer opens with scrim; the sidebar logo now links to `/`. Chose the hamburger+drawer pattern over the BRIEF "Layout shell" 240->56px rail sketch per explicit user direction (a 56px rail still cramps a 393px phone). The remaining per-page dense-grid reflow is the separate follow-up captured above.
